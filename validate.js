// VALIDATE NUMBER

!/^([0-9])*$/.test(numero)

! = permite establecer un negativo, condicion que no se cumple.
/^([0-9])*$/ = acepta cualquier elemento entre 0 y 9.
.test(numero) = valida la sintaxis.
